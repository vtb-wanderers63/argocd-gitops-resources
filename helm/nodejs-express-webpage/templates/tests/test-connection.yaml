apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodejs-express-webpage.fullname" . }}-test-connection"
  labels:
    {{- include "nodejs-express-webpage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodejs-express-webpage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
